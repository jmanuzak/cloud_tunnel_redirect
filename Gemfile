source :gemcutter

gem "sinatra", ">=1.2.6"
gem "datamapper", ">=1.1.0"

group :development do
  gem "dm-sqlite-adapter", ">=1.1.0"
  gem "shotgun"
  gem "rspec"
  gem "rack-test"
end

group :production do
  gem "dm-postgres-adapter", ">=1.1.0"
end
